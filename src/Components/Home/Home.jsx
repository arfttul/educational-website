import React from "react";
import { Outlet } from "react-router-dom";
import Banner from "../Banner/Banner";

const Home = (props) => {
  console.log(props.v);

  return <div></div>;
};

export default Home;
